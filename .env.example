# GitHub Personal Access Tokens
# Used for fetching contribution data from the GitHub GraphQL API
# Create them at: https://github.com/settings/tokens
# Scopes needed: read:user
# For private contribution counts, use a classic PAT with repo scope, or a
# fine-grained PAT that has access to the relevant private repositories.
GITHUB_TOKEN=
ASC_GITHUB_TOKEN=
CINCIN_GITHUB_TOKEN=
JWILLI_GITHUB_TOKEN=
LEBOWSKI_GITHUB_TOKEN=
